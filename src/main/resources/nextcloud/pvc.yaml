apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cloud-pvc
  labels:
    app.kubernetes.io/application: cloud
spec:
  storageClassName: REPLACEME
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: REPLACEME
  selector:
    matchLabels:
      app.kubernetes.io/application: cloud
